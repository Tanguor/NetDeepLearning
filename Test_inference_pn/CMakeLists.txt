################################################################################
# File generated by HDevelop Version 22.11
#
# Do not modify!
################################################################################

cmake_minimum_required(VERSION 3.9 FATAL_ERROR)

enable_language(CSharp)

project(Test_inference_pn CSharp)

if(DEFINED ENV{HALCONROOT} AND NOT $ENV{HALCONROOT} STREQUAL "")
  message("Using HALCONROOT " $ENV{HALCONROOT})
  STRING(REGEX REPLACE "\\\\" "/" HALCONROOT $ENV{HALCONROOT}) 
endif()
set(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} ${CMAKE_CURRENT_SOURCE_DIR}/cmake)


# ==================================================
#                  Source files
# ==================================================
SET(SOURCE source/Test_inference_pn.cs)

set(HDEVENGINE_DLL ${HALCONROOT}/bin/dotnet35/hdevenginedotnetxl.dll)
set(HALCON_DLL ${HALCONROOT}/bin/dotnet35/halcondotnetxl.dll)

add_library(${PROJECT_NAME} ${SOURCE})
set_property(TARGET ${PROJECT_NAME} PROPERTY VS_DOTNET_REFERENCE_hdevengine ${HDEVENGINE_DLL})
set_property(TARGET ${PROJECT_NAME} PROPERTY VS_DOTNET_REFERENCE_halcon ${HALCON_DLL})

set_target_properties(${PROJECT_NAME} PROPERTIES LINKER_LANGUAGE CSharp)

if (NOT ${CUSTOM_CSharpPrototype_RESOURCE_DIR} STREQUAL "")
  message("-- Copy exported resources to " ${CUSTOM_CSharpPrototype_RESOURCE_DIR})
  add_custom_command(TARGET ${PROJECT_NAME} POST_BUILD
                     COMMAND ${CMAKE_COMMAND} -E copy_directory
                             "${CMAKE_CURRENT_SOURCE_DIR}/res_Test_inference_pn"
                             "${CUSTOM_CSharpPrototype_RESOURCE_DIR}")
elseif (NOT ${CMAKE_CURRENT_SOURCE_DIR} STREQUAL ${CMAKE_BINARY_DIR})
  if (${WIN32})
    SET(RESOURCE_TARGET_DIR ${CMAKE_BINARY_DIR}/$<CONFIG>)
  elseif (${APPLE})
    SET(RESOURCE_TARGET_DIR ${CMAKE_BINARY_DIR})
  else()
    SET(RESOURCE_TARGET_DIR ${CMAKE_BINARY_DIR})
  endif()
  message("-- Copy exported resources to " "${RESOURCE_TARGET_DIR}/res_Test_inference_pn")
  add_custom_command(TARGET ${PROJECT_NAME} POST_BUILD
                     COMMAND ${CMAKE_COMMAND} -E copy_directory
                             "${CMAKE_CURRENT_SOURCE_DIR}/res_Test_inference_pn"
                             "${RESOURCE_TARGET_DIR}/res_Test_inference_pn")
endif()



